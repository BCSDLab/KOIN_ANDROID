package in.koreatech.koin.core.networks.interactors;

import in.koreatech.koin.core.networks.ApiCallback;

public interface BokdukInteractor {
    void readBokdukList(final ApiCallback apiCallback);
}
