# Feature Club Module - AGENTS.md

`feature/club` handles club management and Q&A.

## Keep In Mind

- Use Compose and Orbit patterns already established in the module.
- Keep state, side effects, and use case calls separated.
- Use `Result<T>` or `Flow<T>` where the domain layer already does.

## Read First

- Root `AGENTS.md`
- `core/AGENTS.md`
